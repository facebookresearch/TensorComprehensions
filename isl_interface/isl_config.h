// Mock version of isl's isl_config.h configuration file.
// extract_interface.cc expects this file to contain all
// the required defines, but in TC they are set using
// add_definitions and thereby passed on the command line.
